import styled from 'styled-components'

export const Container = styled.div`
  width: 450px;

  .select__control {
    border: 0.5px solid #878688;
    border-radius: var(--border-radius-input);
    padding: 2px;
  }
`
export const Label = styled.label`
  font-size: var(--font-size-sm);
`
